# Default SOEM interface.
# LinuxCNC recommendation: configure SOEM_IFNAME to the real NIC name,
# instead of relying on renaming scripts.
# 启动 HAL 前: set -a; . /etc/rk3588-cnc/soem.env; set +a
SOEM_IFNAME=end1
