# Skeleton HAL — customize PDO mapping in soem_linuxcnc.c for your drives.
# Source SOEM env:  set -a; . /etc/rk3588-cnc/soem.env; set +a

loadusr -Wn soem_linuxcnc soem_linuxcnc
setp soem_linuxcnc.ifname ${SOEM_IFNAME}

# Example: connect to servo thread (uncomment after machine INI exists)
# addf soem_linuxcnc.read-all servo-thread
# addf soem_linuxcnc.write-all servo-thread
